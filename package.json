{
  "name": "internet-weather",
  "version": "0.1.0",
  "private": true,
  "description": "A cinematic realtime weather simulation of the internet.",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "npm-run-all -p dev:server dev:client",
    "dev:client": "npm --workspace client run dev",
    "dev:server": "npm --workspace server run dev",
    "build": "npm --workspace client run build && npm --workspace server run build",
    "typecheck": "npm --workspace client run typecheck && npm --workspace server run typecheck"
  },
  "devDependencies": {
    "@rollup/pluginutils": "^5.3.0",
    "npm-run-all": "^4.1.5"
  }
}
